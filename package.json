{
  "name": "substack-context-capture",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "description": "Chrome extension for capturing Substack articles and selected discussion threads as structured research packages.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check background.js && node --check content.js && node --check content-comments.js && node --check sidepanel.js && node --check note-multi-picker.js && node --check lib/model.js && node --check lib/note-replies.js && node --check lib/note-selection.js && node --check lib/exporters.js && node --check lib/zip.js"
  }
}
