{
  "name": "research-context-capture",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension for capturing X/Substack content and selected discussion context as structured research packages.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check background.js && node --check content.js && node --check content-comments.js && node --check x-capture.js && node --check sidepanel.js && node --check note-multi-picker.js && node --check x-multi-picker.js && node --check lib/model.js && node --check lib/note-replies.js && node --check lib/note-selection.js && node --check lib/exporters.js && node --check lib/zip.js"
  }
}
